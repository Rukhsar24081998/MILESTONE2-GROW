# Lightweight deps for Render free tier (512 MB).
# Semantic embeddings (torch / sentence-transformers) are omitted on purpose —
# the API falls back to keyword / JSONL retrieval and Groq for answers.

# Core
pyyaml>=6.0
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
python-dotenv>=1.0.0

# API & Server
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.0

# Optional local vector store (keyword path works without embeddings)
chromadb>=0.5
numpy>=1.24.0

# LLM
groq>=0.8.0
