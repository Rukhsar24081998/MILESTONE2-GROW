# Tests

| File | Phase |
|------|-------|
| `test_phase0_corpus.py` | 0 — manifest & schemes validation |
| `test_phase1_1_fetcher.py` | 1.1 — allowlist fetcher |
| `golden_queries.json` | 6 — factual / refusal / performance (to add) |

```bash
pytest -v
```
