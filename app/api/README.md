# API (`app/api`)

| Phase | Deliverables |
|-------|----------------|
| 3 | `POST /ask`, request validation |
| 5 | `GET /health`, `GET /corpus/status`, `GET /examples` |

Entry point (Phase 5): `app/api/main.py` (FastAPI).
