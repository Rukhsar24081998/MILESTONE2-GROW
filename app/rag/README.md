# RAG (`app/rag`)

| Phase | Deliverables |
|-------|----------------|
| 2 | `retrieve()`, hybrid search, scheme detection, context builder |
| 3 | Integration with LLM generation |

Debug CLI (Phase 2): `python -m app.rag.debug "<query>"`
